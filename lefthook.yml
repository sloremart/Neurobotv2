# lefthook.yml — verificación local en cada commit
# Como Go NO está instalado localmente, todo corre vía Docker (igual que tu build/test).
# En Windows/Git Bash, MSYS_NO_PATHCONV=1 evita que se corrompa la ruta del volumen.
#
# Activación (una sola vez):
#   1) Instala lefthook:  npm install -g lefthook   (o usa el binario oficial)
#   2) Instala los hooks: lefthook install
#
# La imagen golangci/golangci-lint está basada en golang, así que trae el toolchain Go.
# Pin de versión idéntico al de .golangci.yml y ci.yml.

pre-commit:
  parallel: false
  commands:
    lint-new:
      glob: "*.go"
      # --new-from-rev=HEAD: solo falla por problemas NUEVOS respecto al último commit.
      # Así la deuda histórica del repo no bloquea cada commit desde el día uno.
      # IMPORTANTE: `golangci-lint run` (v2) YA reporta los formateadores declarados en
      # `formatters` (gofumpt/goimports) como issues, así que con el baseline esto también
      # cubre el FORMATO del código nuevo. Por eso NO hay un `format-check` aparte: el antiguo
      # `golangci-lint fmt --diff` escaneaba TODO el repo sin baseline y bloqueaba el commit
      # por la deuda de formato preexistente (la migración nunca se gofumpt-eó).
      run: MSYS_NO_PATHCONV=1 docker run --rm -v "$(pwd):/app" -w /app golangci/golangci-lint:v2.11.0 golangci-lint run --new-from-rev=HEAD

# Nota: dejamos `go vet`, `go test -race` y `govulncheck` para la CI (son más lentos).
# El pre-commit prioriza feedback en segundos: lint+formato de lo nuevo.
# Para formatear todo el repo de una vez (limpiar la deuda histórica), correr manualmente:
#   docker run --rm -v "$(pwd):/app" -w /app golangci/golangci-lint:v2.11.0 golangci-lint fmt ./...
